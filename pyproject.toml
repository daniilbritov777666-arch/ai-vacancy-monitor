[project]
name = "vacancy-monitor"
version = "0.1.0"
description = "Telegram vacancy monitor for AI-assisted freelance tasks"
requires-python = ">=3.12"
dependencies = [
  "beautifulsoup4>=4.12.0",
  "requests>=2.32.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
